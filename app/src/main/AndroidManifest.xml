<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.faculdade">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_48x48"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroFaculdade"
            android:label="@string/title_activity_cadatro_faculdade" />
        <activity
            android:name=".CadastroDisciplina"
            android:label="@string/title_activity_cadastro_disciplina"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListarDisciplina"
            android:label="@string/title_activity_listar_disciplina"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>